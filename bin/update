#!/bin/bash
# allow tag_serer | mapreduce
set -Eeuo pipefail

usage() {
  echo "Usage: $0 (tag_server|mapreduce)"
}

if [ $# -ne 1 ] ; then
  usage
  exit 1
fi

# Parse argument.  $1 is the first argument
case $1 in
  "tag_server")
    
    ;;

  "mapreduce")
    
    ;;
    done
    ;;
  *)
    usage
    exit 1
    ;;
esac
